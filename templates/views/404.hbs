<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="./css/style.css"> 
        <link rel="icon" href="./img/weather.jpg">
    </head>
    <body>
        <div class="maincontent">
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>
        
        {{>footer}}
    </body>
</html>